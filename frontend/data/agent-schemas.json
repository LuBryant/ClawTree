{
  "version": "2026-07-03.ai-schema-v1",
  "schemas": {
    "classify": {
      "type": "object",
      "additionalProperties": false,
      "required": ["labels", "confidence", "sourceIds", "needsReview"],
      "properties": {
        "labels": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["campus", "ai", "web3", "recap", "cooperation", "sports", "finance-literacy", "rwa", "partner", "irrelevant"]
          }
        },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "sourceIds": { "type": "array", "items": { "type": "string" } },
        "needsReview": { "type": "boolean" }
      }
    },
    "dedup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["isDuplicate", "canonicalId", "reason", "confidence"],
      "properties": {
        "isDuplicate": { "type": "boolean" },
        "canonicalId": { "type": ["string", "null"] },
        "reason": { "type": "string" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    },
    "compliance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["riskLevel", "riskLabels", "safeSummary", "diffSummary", "sourceIds"],
      "properties": {
        "riskLevel": { "type": "string", "enum": ["low", "medium", "high"] },
        "riskLabels": { "type": "array", "items": { "type": "string" } },
        "safeSummary": { "type": "string" },
        "diffSummary": { "type": "string" },
        "sourceIds": { "type": "array", "items": { "type": "string" } }
      }
    },
    "match": {
      "type": "object",
      "additionalProperties": false,
      "required": ["score", "subscores", "fitPoints", "conflicts", "missingInfo", "sourceIds"],
      "properties": {
        "score": { "type": "integer", "minimum": 0, "maximum": 100 },
        "subscores": {
          "type": "object",
          "additionalProperties": false,
          "required": ["topic", "audience", "timing", "city", "resources", "completeness"],
          "properties": {
            "topic": { "type": "integer", "minimum": 0, "maximum": 100 },
            "audience": { "type": "integer", "minimum": 0, "maximum": 100 },
            "timing": { "type": "integer", "minimum": 0, "maximum": 100 },
            "city": { "type": "integer", "minimum": 0, "maximum": 100 },
            "resources": { "type": "integer", "minimum": 0, "maximum": 100 },
            "completeness": { "type": "integer", "minimum": 0, "maximum": 100 }
          }
        },
        "fitPoints": { "type": "array", "items": { "type": "string" } },
        "conflicts": { "type": "array", "items": { "type": "string" } },
        "missingInfo": { "type": "array", "items": { "type": "string" } },
        "sourceIds": { "type": "array", "items": { "type": "string" } }
      }
    },
    "proposal": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tiers", "risks", "questions", "sourceIds", "guardrails"],
      "properties": {
        "tiers": {
          "type": "array",
          "minItems": 3,
          "maxItems": 3,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "value", "resources", "nextStep"],
            "properties": {
              "name": { "type": "string", "enum": ["light", "medium", "deep"] },
              "value": { "type": "string" },
              "resources": { "type": "array", "items": { "type": "string" } },
              "nextStep": { "type": "string" }
            }
          }
        },
        "risks": { "type": "array", "items": { "type": "string" } },
        "questions": { "type": "array", "items": { "type": "string" } },
        "sourceIds": { "type": "array", "items": { "type": "string" } },
        "guardrails": {
          "type": "object",
          "additionalProperties": false,
          "required": ["noUnapprovedPrize", "noGuaranteedExposure", "humanApprovalRequired"],
          "properties": {
            "noUnapprovedPrize": { "type": "boolean" },
            "noGuaranteedExposure": { "type": "boolean" },
            "humanApprovalRequired": { "type": "boolean" }
          }
        }
      }
    },
    "reply": {
      "type": "object",
      "additionalProperties": false,
      "required": ["intent", "confidence", "summary", "nextAction", "needsHumanReview"],
      "properties": {
        "intent": { "type": "string", "enum": ["positive", "question", "decline", "ooo", "unknown"] },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "summary": { "type": "string" },
        "nextAction": { "type": "string" },
        "needsHumanReview": { "type": "boolean" }
      }
    }
  }
}
